# 2026-09-24

## 前端改造提示词（A2A OS 控制台）
- 需求：用户改完后端（A2A OS：discover/route/runtime-graph/collaboration/governance/scheduling/tenant/execution/lifecycle），要前端更好呈现，要求先分析代码再产出提示词。
- 分析结论：
  - 前端 `apps/web`（Next.js 15 App Router + TS + Tailwind + reactflow 11 + recharts 3）仍停在 Phase 34；
    `lib/api.ts` 未覆盖任何 Phase 2/3/5 新接口；`docs/api.md` 也未收录新端点。
  - 后端新增可前端化的能力组：发现/路由、运行时协作图（含 WS stream）、执行记录+生命周期、治理拒绝审计、调度/成本/租户。
  - 关键坑：`/v1/agents` 由 Gateway 自己的 registry handler 接管，因此 `GET /v1/agents/{id}/capacity`
    与 `/reliability` **经网关不可达**；网关别名 `/v1/agent-runtime/{id}/*` 只覆盖 health/heartbeat/drain。
- 交付：`FRONTEND-UPGRADE-PROMPT.md`（仓库根目录）。结构 = 背景 / 10 条硬约束 / 前端基线 /
  4.1–4.7 精确 API 契约（含字段与返回结构）/ M0–M7 任务清单 / 全局验收 / 交付格式。
- 前端设计系统备忘（改前端必须复用）：颜色 ink-950/900/800/700、mist-100/200/400、
  signal(#3dffa8)/signal-dim/signal-warm(#ffb454)；字体 font-display(Fraunces)/font-sans(Manrope)/font-mono(IBM Plex Mono)；
  微标签 `font-mono text-[10px] uppercase tracking-[0.18em] text-mist-400`；卡片 `rounded-2xl border border-white/10 bg-ink-900/50 p-4`；
  DAG 状态色见 `components/tasks/TaskFlowDag.tsx` 的 STATUS_COLOR。
- 前端约束备忘：所有请求走 `lib/api.ts` 的 `request<T>()`；WS 必须用 `hooks/useWebSocket.ts` 的 `buildWsUrl()`；
  不要新增 npm 依赖；轮询间隔不要低于 2.5s（Windows 临时端口会被耗尽）。

## 前端改造落地后的复核（同日）
- 提示词已被执行：api.ts 841→1507 行；新增 CollaborationGraph / CollaborationStream / ExecutionPanel / CostPanel /
  GovernancePanel / TenantPanel / SchedulingPageView + `/scheduling`；AppShell NAV 增「调度」；useWebSocket 增 `path` 参数。
  整体质量高：`apiErrorMessage()` 统一错误文案、`getDefaultTenantId()`、`requestOrchPreferred()` 直连降级通道都做了。
- 发现的遗留问题（已告知用户，按 P0/P1/P2 排序）：
  - P0-1 DashboardView 把 4 个新接口放进 8s 轮询的同一个 load()，每轮 9 个请求（含打 DB+Redis 的 scheduling/candidates）。
  - P0-2 CollaborationGraph 的 task 节点（id=`task:<tid>`）没有任何边指向 → 画布呈两个不相连的簇；应按
    caller_agent → task:xxx → target_agent 两段边重连。
  - P0-3 三层轮询叠加：useTaskLive(10s) + CollaborationGraph(10s 无条件) + CollaborationStream(10s/2.5s)。
    CollaborationGraph 的轮询应在 `isConnected` 时停掉。
  - P1-4 Dashboard「调度候选」硬编码 skill=web-search，指标无意义。
  - P1-5 TenantPanel 成本趋势永远单点：`GET /v1/tenants/{id}/cost` 只返回聚合，没有 trend 字段。
  - P1-6 CostPanel 用 `total.tokens ?? total.input_tokens`，漏算 output_tokens（aggregate 只有 input_tokens/output_tokens）。
  - P1-7 root_task_id 直接取 task_id；orchestrator 播种时 root==task_id 所以能跑，但应
    `task.execution?.execute?.root_task_id ?? task_id`。
  - P2-8 `components/TaskMonitor.tsx` 是死代码（无人 import），其改动无效；同类死代码还有 TaskWorkspace /
    TaskDag / SiteNav / AgentsPanel / WorkflowsPanel / MarketplacePanel / HomeComposer。
  - P2-9 capacity/reliability 走网关不可达，需在网关补代理或设 `NEXT_PUBLIC_ORCHESTRATOR_BASE`。
- 事实核对：`ExecutionEvent` 字段是 `timestamp`（不是 ts）、`sequence` 在 `payload.sequence`；
  `classify()` 返回 `{class, record}`；`cost.aggregate()` 返回 count/estimated_cost/input_tokens/output_tokens/gpu_seconds/wall_time_ms/currency（**无** trend、**无** tokens）。

## P0–P2 修复复核（同日 17:53，静态审查 + tsc 通过 EXIT=0）
- 已修复：P0-1（Dashboard OS KPI 拆独立 effect，60s，allSettled）、P0-2（协作图 link 拆 caller→task→target 两段边）、
  P0-3（CollaborationGraph 仅 `!wsConnected` 时 10s 轮询 + WS 事件 800ms 防抖刷新）、P1-4（KPI 卡改为「在线 Agent」）、
  P1-5（TenantPanel 改用 `/v1/billing/usage` daily_tasks 画趋势，dataKey 动态切换）、P1-6（tokenTotal = input+output，兼容 tokens）、
  P1-7（root_task_id 取 `execution.execute.root_task_id`）、P2-8（TaskMonitor diff 已还原）、P2-10（simulate JSON.parse try/catch 行内报错）、
  P2-11（onNodesChange no-op）。
- 未解决：P2-9 —— `.env.local` 只有 API_BASE/API_KEY，无 `NEXT_PUBLIC_ORCHESTRATOR_BASE`，
  capacity/reliability 仍走网关被 registry 拦截 → 面板必为空态。需加 env（并重启 dev server，NEXT_PUBLIC 编译期内联）。
