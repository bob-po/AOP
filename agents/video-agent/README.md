# Video Agent

Text-to-video stub (`text-to-video`). Returns storyboard metadata; swap with AIVE later.

```bash
cd agents/video-agent
pip install -r requirements.txt
uvicorn agent:app --host 0.0.0.0 --port 8006
```
