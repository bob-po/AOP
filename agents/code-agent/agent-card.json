{
  "name": "Code Agent",
  "description": "Sandboxed Python expression evaluator under seccomp / no-new-privileges (AST-safe, no imports).",
  "url": "http://127.0.0.1:8007/",
  "version": "0.1.0",
  "protocolVersion": "0.3.0",
  "capabilities": {
    "streaming": false,
    "pushNotifications": false
  },
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["text", "application/json"],
  "skills": [
    {
      "id": "code-execution",
      "name": "Code Execution",
      "description": "Evaluate a restricted Python expression in an isolated sandbox",
      "tags": ["code", "sandbox", "high-risk"],
      "examples": ["sum(range(10))", "sqrt(2) * 3"],
      "inputModes": ["text"],
      "outputModes": ["text", "application/json"]
    }
  ]
}
