services:
  app:
    build: .
    ports:
      - "${PORT:-3000}:${PORT:-3000}"
    environment:
      PORT: ${PORT:-3000}
      HOST: 0.0.0.0
    restart: unless-stopped
    mem_limit: 512m
    cpus: 1.0
    pids_limit: 256
