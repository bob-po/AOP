{
  "name": "Start Agent (DeployPilot)",
  "description": "A2A façade for DeployPilot: analyze/plan/deploy projects and collaborate with peer Agents.",
  "url": "http://127.0.0.1:8010/",
  "version": "0.1.0",
  "protocolVersion": "0.3.0",
  "agentId": "aop-start-agent-v1",
  "agentKey": "start-agent",
  "capabilities": { "streaming": true },
  "defaultInputModes": ["text"],
  "defaultOutputModes": ["text", "application/json"],
  "skills": [
    {
      "id": "project-deploy",
      "name": "Project Deploy",
      "description": "Analyze a GitHub URL or local path and produce a deployment plan / invoke DeployPilot",
      "tags": ["deploy", "docker", "devops", "start"],
      "examples": [
        "Deploy https://github.com/user/repo with docker",
        "Analyze ./my-project and propose a start plan"
      ],
      "inputModes": ["text"],
      "outputModes": ["text", "application/json"]
    }
  ],
  "runtime": {
    "framework": "fastapi",
    "pythonVersion": "3.11+",
    "timeoutSeconds": 600,
    "notes": "TypeScript DeployPilot CLI remains the engine; this process is the A2A Server+Client façade."
  },
  "metadata": {
    "author": "AOP Platform",
    "engine": "DeployPilot"
  }
}
