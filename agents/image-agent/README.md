# Image Agent

Text-to-image stub (`text-to-image`). Returns SVG promo art for local demos; swap with ComfyUI later.

```bash
cd agents/image-agent
pip install -r requirements.txt
uvicorn agent:app --host 0.0.0.0 --port 8005
```
